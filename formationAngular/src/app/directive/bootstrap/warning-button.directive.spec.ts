import { WarningButtonDirective } from './warning-button.directive';

describe('WarningButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new WarningButtonDirective();
    expect(directive).toBeTruthy();
  });
});
